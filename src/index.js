import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

const name="josh perez"
const element = <h1>hello worlds{name}</h1>
ReactDOM.render(
  element,
  document.getElementById('root')
);
